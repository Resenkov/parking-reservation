package resenkov.work.parkinguserservice.filter;

/**
 * Legacy filter kept for backward compatibility.
 * JWT auth is handled by Spring Security OAuth2 Resource Server configuration.
 */
public final class JwtAuthFilter {
    private JwtAuthFilter() {
    }
}
